export const isString = (item: string | unknown) => typeof item === 'string';
export const isNumber = (item: number | unknown) => typeof item === 'number';
export const isObject = (item: object | unknown) => typeof item === 'object';
